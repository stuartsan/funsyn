'use strict';[Array,Date,Object,RegExp,String].forEach(function(type){var proto=type.prototype;exports[type.name] = {};Object.getOwnPropertyNames(proto).filter(function(propName){return typeof proto[propName] === 'function';}).forEach(function(propName){var makeFn=new Function('return function ' + propName + ' (instance) {\n        return ' + type.name + '.prototype.' + propName + '\n                .apply(instance, [].slice.call(arguments, 1));\n      }');exports[type.name][propName] = makeFn();});});
